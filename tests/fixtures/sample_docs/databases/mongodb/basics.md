# MongoDB basics

MongoDB is a document-oriented NoSQL database. Data is stored as BSON
documents grouped into collections, instead of rows in tables. It has no
fixed schema by default, which makes it flexible for evolving data shapes.
