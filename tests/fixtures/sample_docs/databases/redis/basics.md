# Redis basics

Redis is an in-memory key-value store, often used as a cache or a message
broker. Because data lives in RAM, reads and writes are extremely fast, but
data size is limited by available memory.
